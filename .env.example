# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase service role (required for account deletion)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Site URL (required, absolute origin only, e.g. https://neuroflow.app)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Rate limit backend: auto | supabase | memory
# - auto: use Supabase shared limiter when configured, else memory
# - supabase: force shared limiter (falls back to memory on runtime errors)
# - memory: single-instance limiter only
RATE_LIMIT_BACKEND=auto

# Anthropic API key (required for AI features)
ANTHROPIC_API_KEY=sk-ant-your-key
